import React from 'react'
import ReactDOM from 'react-dom/client'
import QRky from './components/QRky.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <QRky />
  </React.StrictMode>,
)
